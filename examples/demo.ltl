// examples/demo.ltl — runnable demo for ltl_k8s_probe

import std::io
use ltl_k8s_probe as lib

fn main() {
    io::println("ltl_k8s_probe demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}