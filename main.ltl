// ltl-k8s-probe/main.ltl
//
// Kubernetes liveness / readiness probe helper lib.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-k8s-probe v0.1.0")
    io::println("Kubernetes liveness / readiness probe helper lib.")
    0
}
